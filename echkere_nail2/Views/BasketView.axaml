<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="echkere_nail2.BasketView"
			 x:DataType="vm:BasketViewModel"
			xmlns:vm="using:echkere_nail2.ViewModels">
	
	<Grid RowDefinitions="*, *, Auto">

		<DataGrid IsReadOnly="True"
				  Name="MainDataGridBaskets"
				  Grid.Row="0"
				  ItemsSource="{Binding Baskets}"
				  DoubleTapped="DataGrid_DoubleTapped">
			<DataGrid.Columns>
				<DataGridTextColumn Header="User Full Name" Binding="{Binding UserName}" />
				<DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}"/>
				<DataGridTextColumn Header="Count" Binding="{Binding Count}" />
			</DataGrid.Columns>
		</DataGrid>

		<ContentControl Name="MainContentControl"
						Grid.Row="1" />

		<Button Content="Добавить"
				Grid.Row="2"
				Click="Button_Click"/>

		<Button Content="Удалить"
            Grid.Row="3"
            Click="Delete_Click"
				HorizontalAlignment="Right"
				Grid.Column="1"/>

	</Grid>
</UserControl>
